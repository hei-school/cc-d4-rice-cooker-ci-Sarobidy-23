version: 2.1

jobs:
  rice-cooker-test:
    docker:
      - image: cimg/node:16.17.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: "npm install"

      - run:
          name: Run tests
          command: "npm test"

workflows:
  rice-cooker-workflow:
    jobs:
      - rice-cooker-test